# 🚀 Smart File Organizer - 技術開發里程碑 (2026/04/29 - 05/08)

這份文件彙整了本專案在決賽前的核心技術突破，旨在確保全體組員對系統架構、效能優化與 AI 邏輯具備一致的認知。

## 🛠 技術堆疊 (Technical Stack)
- **語言**: C++ 17
- **框架**: Qt 6.x (GUI, Multithreading, File System Watcher)
- **AI 引擎**: llama.cpp (GGUF 格式)
- **硬體加速**: Apple Metal API (專為 M4 晶片優化)

---

## 📈 三大核心技術里程碑

### 1. 記憶體防護與 AI 生命週期管理
* **技術挑戰**: LLM 模型動態載入會霸佔 4GB+ 的 RAM，長時間開啟會造成系統卡頓。
* **解決方案**: 實作 **Lazy Loading (延遲載入)** 與 **Timeout Unloading (閒置釋放)**。
* **核心邏輯**: 模型僅在分析時載入，若閒置超過 3 分鐘，系統會自動呼叫 `llama_free` 釋放記憶體，並使用 `atomic` 旗標確保執行緒安全。
* **戰功**: 確保低配備電腦也能順暢運行，將 RAM 占用率在閒置時降至趨近於零。

### 2. 輕量級人類回饋強化學習 (RLHF)
* **技術挑戰**: AI 產出的標籤偶爾會不精準或產生幻覺。
* **解決方案**: 建立 **標籤黑名單機制 (Rejected Tags)**。
* **核心邏輯**: 當使用者手動刪除某個標籤時，系統會將其寫入 `rejected_tags.json`。下次分析時，系統會將黑名單塞入 Prompt，動態限制 AI 產生重複的錯誤。
* **戰功**: 實現了「越用越聰明」的客製化分析效果。

### 3. 檔案操作安全防線 (Safety Archiving & Undo)
* **技術挑戰**: 實體歸檔（搬移檔案）具備不可逆的危險性。
* **解決方案**: 實作 **Undo Stack (撤銷堆疊)** 與 **Root Protection (根目錄防護)**。
* **核心邏輯**: 使用 `std::pair` 記錄搬移前後的路徑。若使用者反悔，系統會依據 Inode 紀錄將檔案搬回原位。此外，系統禁止對桌面、系統槽執行歸檔，防止系統崩潰。
* **戰功**: 將檔案管理從「虛擬標籤」提升到「實體管理」，且具備企業級的容錯能力。

---

## 🎨 UI/UX 架構進化
- **TDI 分頁系統**: 從混亂的彈出視窗升級為「瀏覽器式分頁」，整合【核心工作區】、【冗餘清理】與【圖譜分析】。
- **進度節流技術 (Throttling)**: 解決了背景計算 SHA-256 時產生的 UI 飢餓問題，確保進度條顯示時畫面不卡頓。
- **智慧目錄監控**: 實作 `QFileSystemWatcher` 與 `Debounce` 演算法，能主動感知外部檔案變動並提醒，但不產生迴圈觸發。

### 4. 標籤數據正規化與來源識別 (Sprint 4)
* **技術挑戰**: 系統標籤大小寫混亂（如 Report 與 report 共存），且 AI 標籤與使用者標籤混淆。
* **解決方案**: 實作資料層的字串清洗與 UI 層的來源標記。
* **核心邏輯**: 
  - 封裝 `normalizeTag` 函式，統一執行 `trimmed()` 與 `toLower()`。
  - 對 `LlamaEngine` 產出的標籤統一加上 `[AI]` 前綴，並在 UI 渲染時進行視覺區隔與去重疊（優先顯示手動標籤）。
* **戰功**: 奠定了資料庫等級的數據一致性，防止關聯圖譜與實體歸檔因髒資料 (Dirty Data) 崩潰。

### 5. 國際化架構 (i18n) 與模型熱切換 (Sprint 5)
* **技術挑戰**: 系統需具備動態多語系切換能力以面向國際評審，且需支援動態載入不同量級的 AI 模型。
* **解決方案**: 實作 `LanguageManager` 單例模式與動態設定中心 (`SettingsDialog`)。
* **核心邏輯**: 
  - 將硬編碼 (Hardcoded) 的 UI 字串抽離，透過 `getText()` 根據當前狀態 (ZH_TW / EN_US) 動態渲染。
  - 實作持久化設定檔，允許在 Runtime 切換 `.gguf` 模型路徑（導入 **Llama-3.1-8B-Instruct Q4_K_M** 以配適 M4 統一記憶體），並傳遞指標給背景重新載入。
* **戰功**: 奠定了產品邁向全球化的底層架構，並大幅降低測試不同 LLM 模型的除錯成本。

### 5.1 UI 國際化優化與模型量化決策 (Sprint 5 補遺)
* **技術挑戰**: 解決動態切換語言時 UI 渲染不完全的問題，並選擇最適合邊緣運算的 AI 模型。
* **解決方案**: 
  - 導入 `tr()` 全域包裹機制與 `retranslateUi` 動態刷新邏輯。
  - 選定 **Llama-3.1-8B-Instruct Q4_K_M** 作為核心引擎。
* **戰功**: 
  - 確保了系統在不重啟的情況下能 100% 切換中英文介面。
  - 透過 Q4_K_M 量化技術，將 80 億參數模型壓縮至 5GB 內，在保持高效能推論的同時，完美適配 MacBook M4 的統一記憶體架構。


---

### 6.2 防幻覺 (Anti-Hallucination) 與類型守門員
* **技術挑戰**: AI 面對不可讀的二進位檔案 (如 PDF, MP4) 會根據檔名產生語意幻覺，輸出錯誤摘要。
* **解決方案**: 實作「檔案類型白名單」與「動態 Prompt 攔截機制」。
* **核心邏輯**: 
  - 建立文字檔白名單 (txt, md, cpp...)，僅對白名單檔案執行全文讀取。
  - 針對非白名單檔案，攔截讀取動作，改發送「限縮式 Prompt」，命令 AI 誠實告知無法讀取內容，並僅依據檔名進行分類。
* **戰功**: 顯著提升了系統的資訊真實度，解決了 LLM 在邊緣運算環境下常見的瞎掰問題，強化了系統的可靠性。

### 6.3 結構化輸出 (JSON) 防護網與 LLM 幻覺對抗
* **技術挑戰**: Llama 8B 模型在輸出時常伴隨 markdown 標籤、破格 JSON (`{,`, `,"",`)、或是將摘要拆分為多段 JSON，導致 C++ 原生 `QJsonDocument` 解析崩潰 (Fallback 發動並產生過長亂碼標籤)。此外，英文輸出常帶有死板的模板句型。
* **解決方案**: 實作「強硬提示詞工程」搭配「正則表達式容錯解析器」。
* **核心邏輯**: 
  - **Prompt 強化**: 禁用描述性字眼 (如 document, file, contains)，強制提供主題句範例。
  - **多段合併解析**: 使用 Regex `\{.*\}` 抽出所有片段逐一清洗，修復多餘逗號與空字串。
  - **標籤硬限制**: 實作 3 個標籤的 Hard Limit、長度上限 (15字元)、並過濾無意義單字 (如 `ai`)。
* **戰功**: 成功馴服 8B 模型，確保系統在中英文環境下皆能 100% 穩定輸出乾淨、具備高度商業價值的「一句話摘要」與「精準短標籤」。

### 7.1 極限檔案支援與標籤語意正規化
* **技術挑戰**: 系統原先無法解析 `.xlsm` 等 Office 衍生格式，且 AI 輸出的英文/同義標籤（如 database, sql）會與系統預設的中文標籤（如 資料庫）產生雙生重疊，導致 UI 樹狀圖與實體歸檔混亂。
* **解決方案**: 擴充 OpenXML 共用解析器，並實作「標籤同義詞字典 (Synonym Alias Mapping)」。
* **核心邏輯**: 
  - 將 `.xlsx`, `.xlsm`, `.doc`, `.pptx` 納入文字提取白名單，共用 ZIP/XML 拆解邏輯，並限制 3000 字上限以保護 AI Token 上下文。
  - 在 `TagManager::normalizeTag()` 實作攔截映射。當 AI 標籤匹配到系統預設標籤同義詞時，自動剝離 `[AI]` 前綴並歸一化為中文主鍵，實現 MVC 資料層的無縫合併。
* **戰功**: 徹底解決了標籤庫的語意污染問題，並將文件解析支援度涵蓋了 90% 以上的企業常用微軟格式。

### 7.2 修復 KV Cache 記憶污染與 AI 標籤過度泛化
* **技術挑戰**: 在連續分析多個檔案時，AI 引擎會發生上下文記憶洩漏 (Context Leak)，導致上一份檔案的摘要出現在新檔案中。此外，AI 容易產生「過度泛化 (Over-generalization)」的幻覺，例如將一份普通的「教師資格計算表」，僅因包含學術詞彙，就錯誤地貼上無關的「台綜大」這種廣泛機構標籤。
* **解決方案**: 實作推論前強制清空 KV Cache，並導入強硬的標籤精確度提示詞 (Strict Functional Tagging)。
* **核心邏輯**: 
  - **記憶重置**: 在 `LlamaEngine` 每次呼叫 `analyzeFile` 準備送出新 Prompt 前，強制執行 `llama_kv_cache_clear(ctx)`，確保 AI 每次皆以「全新狀態」分析檔案，阻斷記憶污染。
  - **精確度限制**: 於 System Prompt 中加入硬性規則，要求標籤必須描述「具體的檔案功能（如 計分表、測驗卷）」，嚴禁使用廣義的場域或機構標籤（如 學校、台綜大、工作）。
* **戰功**: 徹底消除了邊緣運算環境下 LLM 連續推論的「精神分裂」與記憶殘留問題，確保標籤分類的絕對客觀與精準。

### 7.3 LLM 狀態隔離與 Prompt Contamination (提示詞污染) 修復
* **技術挑戰**: 系統在連續分析無關檔案時，會殘留上一個專案的特定標籤（例如將教師履歷標記為「台綜大」）。初期誤判為 KV Cache 記憶體洩漏，經深度追蹤後，確認為「提示詞污染 (Prompt Contamination)」——系統過度餵給 AI 全局歷史標籤庫 (`existingTags`)，導致模型產生依賴性幻覺。
* **解決方案**: 實作「無狀態推論 (Stateless Inference)」與「零樣本生成 (Zero-shot Generation)」。
* **核心邏輯**: 
  - **切斷提示詞污染**: 在 `MainWindow::analyzeFile()` 中，停止向 AI 注入全局 `existingTags`，強迫模型僅基於「當下檔案內容」進行 Zero-shot 標籤生成。
  - **雙重狀態隔離**: 結合底層的 `llama_kv_cache_clear`，確保每一份檔案在「上下文記憶」與「提示詞參考」雙層面上達到絕對的狀態隔離。
  - **撤除硬性黑名單**: 移除原先用於阻擋機構名稱的 Hard-coded 黑名單，保留大型語言模型對真實相關機構（如確為台綜大公文）的判斷彈性，避免誤殺 (False Negatives)。
* **戰功**: 徹底解決了邊緣 AI 連續處理多檔案時的語意污染問題，達成 100% 的無狀態精準推論。

### 8. 全自動批次掃描與佇列節流機制 (Sprint 8)
* **技術挑戰**: 若一次將大量檔案送交 LLM 推論，將導致邊緣設備記憶體耗盡 (OOM) 且 Qt UI 執行緒凍結。同時需避免批次過程中，使用者的 UI 操作干擾正在分析的檔案指標 (Race Condition)。
* **解決方案**: 實作「事件驅動任務佇列 (Event-Driven Task Queue)」與「狀態鎖定機制」。
* **核心邏輯**: 
  - **單線程接力佇列**: 透過 `QQueue` 暫存任務，並利用 `QTimer::singleShot` 延遲觸發下一個分析，確保主執行緒有足夠時間渲染進度條 (Progress Bar) 畫面，達成零卡頓體驗。
  - **UI 節流渲染**: 批次過程中關閉高耗能的樹狀圖全視圖重繪，改為輕量級的數字累加，待全數完成後再一次性刷新 (Batch UI Update)。
  - **指標鎖定**: 導入 `m_currentAnalyzingFile` 狀態變數，與 UI 選取事件徹底脫鉤，防止非同步推論完成時發生標籤錯置。
* **戰功**: 成功賦予系統「無人值守 (Unattended)」的批次處理能力，大幅提升大規模檔案整理的實用價值，並確保 M4 晶片的記憶體水位始終保持平穩。

### 8.2 UI/UX 深度優化與標籤緩衝機制
* **技術挑戰**: 批次分析時頻繁的 UI 狀態切換導致畫面閃爍，且標籤逐一更新造成檢視壓力。標籤前綴在特殊情況下發生截斷。
* **解決方案**: 導入「非同步緩衝寫入」與「平滑進度條動畫」。
* **核心邏輯**: 
  - **背景非阻塞作業**: 取消全域 UI 禁用，改為背景靜默佇列，允許用戶在分析時並行操作其他功能。
  - **批次寫入 (Bulk Commit)**：實作 `m_pendingResults` 緩衝區，在全數分析完成後才觸發一次性 I/O 與 UI 重繪，大幅減少主執行緒負擔。
  - **前綴邏輯修正**: 使用 `QString::replace` 代替索引切割，根除標籤殘留 `[` 的 Bug。
* **戰功**: 顯著提升了專業化工具的體驗質感，使 AI 分析從「阻斷式流程」轉變為「流暢背景任務」。

---

## 📌 未來擴充備忘錄 (Memo)
* **[ ] 智慧標籤整合功能**: 當標籤庫過於龐大時，實作 AI 驅動的「語意聚合」，將相似標籤 (如：Report vs 報告) 自動提示合併。
* **[ ] 全域背景全自動掃描**: 結合檔案監聽器，實現檔案落地即自動背景分析的「零點擊」體驗。

### 8.3 標籤治理 (Tag Governance) 與動態排序機制
* **技術挑戰**: AI 批次分析易產生大量同義但字串相異的「冗餘標籤 (Tag Explosion)」，導致左側樹狀目錄過於冗長，使用者難以尋找核心分類。
* **解決方案**: 實作「動態頻率排序 (Frequency Sorting)」與「無縫右鍵合併 (Manual Tag Merging)」機制。
* **核心邏輯**: 
  - **頻率優先排序**: 覆寫 `QTreeWidget` 的預設排序邏輯，AI 標籤優先依照「包含檔案數 (Count)」由大到小降冪排序，確保高頻核心標籤浮現於視覺焦點。
  - **MVC 狀態無縫合併**: 於標籤樹實作右鍵 Context Menu。當執行合併時，底層 `TagManager` 會遍歷並覆寫檔案的標籤屬性，並在內存與 JSON 存儲中徹底銷毀舊有標籤的索引，最後觸發單向資料流刷新 UI，確保資料庫絕對的正規化與一致性。
* **戰功**: 賦予使用者強大的資料庫治理能力，解決了非結構化資料 (Unstructured Data) 常見的分類碎裂問題。

### 8.4 AI 標籤自動收斂與多執行緒非阻塞推論
* **技術挑戰**: 手動合併大量冗餘的 AI 標籤耗時費力。若直接要求 LLM 進行全域標籤分析，其推論時間會導致主執行緒 (Main Thread) 凍結，嚴重影響用戶體驗。
* **解決方案**: 實作 `QtConcurrent` 非同步推論與 JSON 結構化合併引擎。
* **核心邏輯**: 
  - 提取全域 `[AI]` 標籤清單，透過 `QtConcurrent::run` 將 Llama 8B 的推論任務派發至背景執行緒 (Background Thread)。
  - 利用 Prompt Engineering 強制模型輸出 Key (冗餘標籤) 與 Value (目標標籤) 的 JSON 對應表。
  - 主執行緒透過訊號接收解析結果，並批次執行底層 `mergeTag`，達成資料庫的無縫收斂。
* **戰功**: 完成了標籤生命週期的「全自動閉環」，展現了系統具備高度的「自我資料治理 (Self-Governance)」能力，且完美維持了 UI 的極致流暢度。

### 8.5 MVC 架構下的 UI 呈現層與資料層解耦 (View-Model Decoupling)
* **技術挑戰**: `[AI]` 標籤前綴在字串處理時易導致中文字元截斷，且在專屬 AI 分頁中重複顯示前綴會造成嚴重的視覺干擾與冗餘。
* **解決方案**: 實作「呈現層前綴隱藏 (Visual Prefix Stripping)」。
* **核心邏輯**: 
  - 保留 `Qt::UserRole` 中完整的標籤主鍵 (Canonical Tag, 含 `[AI]`) 以維持資料庫一致性。
  - 在渲染 UI (`updateTagList`) 與過濾器選單時，動態剝離 `[AI]` 前綴，達成乾淨的視覺體驗。
  - 於 `QInputDialog` 合併選單中導入 `QMap` 映射機制，確保顯示乾淨文字的同時，底層仍傳遞正確的完整標籤鍵。
* **戰功**: 徹底消滅字串截斷 Bug，並大幅提升介面的現代化與專業度。



## 🌍 深度技術探討：跨國軟體架構 (i18n) 與 MVC 顯示解耦

在實作系統中英動態切換的過程中，我們遇到了 7 項跨國軟體開發的經典架構挑戰，並逐一實作了底層解法：

### 1. Qt 原生 `tr()` 機制與自製字典的衝突
* **現象**：許多 UI 字串即使包裹了 `tr()`，切換英文後仍顯示中文。
* **根因**：`tr()` 必須配合 Qt 官方的 `.ts/.qm` 翻譯檔資源庫運作，無法單憑字串直接轉換。
* **解法**：全面改走 `LanguageManager::getText(key)` 自製字典模式，並透過 `updateAllTexts()` 函式將字串重新寫回 UI。

### 2. 靜態文字 vs. 動態文字生命週期
* **現象**：狀態列、虛擬標籤、檔案數等文字，會在掃描流程中反覆被覆蓋回中文。
* **根因**：這些文字並非僅在初始化時設定一次，其動態更新路徑中仍存在硬編碼的中文。
* **解法**：將所有動態更新路徑的 `setText` 替換為 `LanguageManager` 讀取，並確保切換語言時能一併刷新「正在顯示的動態字串」。

### 3. MVC 架構衝突：虛擬標籤的資料與顯示脫鉤 (Deep Issue)
* **現象**：若直接將標籤翻譯為英文，底層的篩選邏輯與關聯圖譜會徹底斷裂。
* **根因**：Tag 標籤同時兼具了「資料庫主鍵 (Key)」與「展示層文字 (Display)」兩種職責。
* **解法**：嚴格實作 MVC (Model-View-Controller) 模式。**資料層**透過 `itemData` 永久保留原始中文 Tag 確保關聯不斷；**展示層**則透過翻譯函式，僅改變 UI 上的視覺文字。

### 4. Emoji 前綴與多字元解析災難 (Surrogate Pairs)
* **現象**：「📄 文件」、「⚙️ 設定」等包含 Emoji 的標籤在部分地方翻譯失敗。
* **根因**：Emoji 在 C++ 記憶體中常以 Surrogate Pair 或 Variation Selector 形式存在，若單純使用字串切割 (Substring) 會導致 Base 字串亂碼。
* **解法**：實作健壯的多字元 Emoji 前綴解析器，將非字母/數字的圖示前綴抽出，僅翻譯剩餘的 Base 字串，最後再組合回 UI 顯示。

### 5. 動態 UI 元件 (Menus / ComboBoxes) 的漏翻
* **現象**：右鍵選單、下拉式篩選器等動態生成的元件切換語言後未更新。
* **根因**：這些元件不在靜態控件清單內，每次觸發 Rebuild 時又被重新塞入原始中文。
* **解法**：為動態建立的 UI 綁定 `languageChanged` 事件，在語言切換時觸發重新建構或重設文字邏輯。

### 6. TDI 分頁架構下的獨立視窗生命週期
* **現象**：主視窗 (`MainWindow`) 翻譯成功，但 `DuplicateCleaner` 與 `GraphWidget` 分頁未跟著變動。
* **根因**：各分頁是獨立的 Widget 實例，擁有自己的 Label 與生命週期。
* **解法**：在各分頁內部分別實作 `updateTexts()` 函式，並在全域語言變更時，由主視窗向所有子分頁廣播更新訊號。

### 7. 邊界條件 (Edge Cases) 提示字串補全
* **現象**：部分「不可分析/不可預覽」等極端情況提示永遠是中文。
* **根因**：這些字串散落在例外處理 (Exception Handling) 分支中，屬於隱藏的硬編碼。
* **解法**：全面盤點邏輯分支，補齊 LanguageManager 的字典 Key 並動態取值。


